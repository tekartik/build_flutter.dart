name: tekartik_build_flutter
description: Helper for building flutter project
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.14.4 <3.0.0'

dependencies:
  path: ^1.8.0
  process_run:
  args:
  tekartik_common_utils:
    git:
      url: git://github.com/tekartik/common_utils.dart
      ref: null_safety
    version: '>=0.13.2'

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
